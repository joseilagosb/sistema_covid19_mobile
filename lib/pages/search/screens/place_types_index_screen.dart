import 'package:flutter/material.dart';

class PlaceTypesIndexScreen extends StatelessWidget {
  const PlaceTypesIndexScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("indice de tipos de lugar");
  }
}
