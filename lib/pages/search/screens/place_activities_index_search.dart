import 'package:flutter/material.dart';

class PlaceActivitiesIndexScreen extends StatelessWidget {
  const PlaceActivitiesIndexScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("indice de actividades de lugar");
  }
}
