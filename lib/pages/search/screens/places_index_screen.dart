import 'package:flutter/material.dart';

class PlacesIndexScreen extends StatelessWidget {
  const PlacesIndexScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("indice de lugares");
  }
}
