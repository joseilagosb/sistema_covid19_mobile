import 'package:flutter/material.dart';

class MarkersVisibilityToggler extends StatelessWidget {
  const MarkersVisibilityToggler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
